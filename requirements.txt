# PROVCHAIN — Dependencies
# ===================================

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# Configuration & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# File upload support
python-multipart>=0.0.9

# Firebase Admin SDK
firebase-admin>=6.5.0

# HTTP client (for external API calls)
httpx>=0.27.0

# Phase 2 — Asset Registration
imagehash>=4.3.0              # Perceptual hashing (pHash)
Pillow>=10.0.0                # Image processing & normalization
google-genai>=1.0.0           # Gemini API (multimodal embeddings)
opentimestamps-client>=0.7.0  # Bitcoin blockchain timestamping
pypdf>=5.0.0                  # PDF text extraction for search summaries

# Phase 3 — Propagation Monitoring
numpy>=1.26.0                 # High-performance cosine similarity for embeddings

# Phase 4 — Evidence Bundle Generation
reportlab>=4.0.0              # PDF generation
matplotlib>=3.8.0             # Propagation charts
jinja2>=3.1.0                 # Legal notice templating
google-api-python-client>=2.100.0  # Gmail API
google-auth-httplib2>=0.1.0        # Google Auth HTTP
google-auth-oauthlib>=1.0.0        # Google Auth OAuthlib

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
