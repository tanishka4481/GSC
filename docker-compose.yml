version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8080"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8080 --reload
